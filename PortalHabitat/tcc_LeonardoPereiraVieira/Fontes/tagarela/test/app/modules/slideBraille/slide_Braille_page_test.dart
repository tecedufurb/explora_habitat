import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:tagarela/app/modules/braille/slideBraille/slide_Braille_page.dart';

void main() {
  testWidgets('SlidePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(SlidePage(title: 'Slide')));
    //  final titleFinder = find.text('Slide');
    //  expect(titleFinder, findsOneWidget);
  });
}
