import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:tagarela/app/components/GestureAccessibility/gesture_accessibility_widget.dart';

void main() {
//  testWidgets('GestureAccessibilityWidget has message', (tester) async {
//    await tester.pumpWidget(buildTestableWidget(GestureAccessibilityWidget()));
//    final textFinder = find.text('GestureAccessibility');
//    expect(textFinder, findsOneWidget);
//  });
}
