import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:tagarela/app/modules/config/config_controller.dart';
import 'package:tagarela/app/modules/config/config_module.dart';

void main() {
  initModule(ConfigModule());
  // ConfigController config;
  //
  setUp(() {
    //     config = ConfigModule.to.get<ConfigController>();
  });

  group('ConfigController Test', () {
    //   test("First Test", () {
    //     expect(config, isInstanceOf<ConfigController>());
    //   });

    //   test("Set Value", () {
    //     expect(config.value, equals(0));
    //     config.increment();
    //     expect(config.value, equals(1));
    //   });
  });
}
